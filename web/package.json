{
	"name": "web",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.10.7",
		"octokit": "catalog:"
	},
	"devDependencies": {
		"wrangler": "^4.51.0"
	}
}
