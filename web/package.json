{
	"name": "web",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"hono": "^4.11.5",
		"octokit": "catalog:"
	},
	"devDependencies": {
		"wrangler": "^4.60.0"
	}
}
